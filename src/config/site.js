/** Public site config — values come from Vite env at build time. */
export const GITHUB_USERNAME =
  import.meta.env.VITE_GITHUB_USERNAME || "Sishir101";

/** Formspree form id (from https://formspree.io) — optional until you create a form */
export const FORMSPREE_FORM_ID = import.meta.env.VITE_FORMSPREE_FORM_ID || "";

export const CONTACT_EMAIL = "sanbadsishirranjan772@gmail.com";
